const markov = {
	// saveMessage: async (parent, {
	// 	userId, guildId, username, content, channelId,
	// }, ctx, info) => ctx.db.createMessage({
	// 	data: {
	// 		userId,
	// 		guildId,
	// 		username,
	// 		content,
	// 		channelId,
	// 	},
	// }, info),
};

module.exports = { markov };
